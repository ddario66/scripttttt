--[[ ddariuspr gui v7 TABS | Early Access Notice | Ride A Pet ]]
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local YT_LINK = "https://www.youtube.com/@ddariuspr"
local DISCORD_LINK = "https://discord.gg/Rp85d5cC"

getgenv().ddariuspr = getgenv().ddariuspr or {}
local State = getgenv().ddariuspr

pcall(function()
	local o = LocalPlayer.PlayerGui:FindFirstChild("ddariuspr_gui")
	if o then o:Destroy() end
end)

local parent = LocalPlayer:WaitForChild("PlayerGui")
local gui = Instance.new("ScreenGui")
gui.Name = "ddariuspr_gui"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = parent

local function corner(o, r)
	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, r)
	c.Parent = o
end

local function stroke(o, col, t)
	local s = Instance.new("UIStroke")
	s.Color = col
	s.Thickness = t
	s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	s.Parent = o
	return s
end

local function copyLink(link, btn, orig)
	local ok = false
	pcall(function() setclipboard(link) ok = true end)
	if not ok then pcall(function() toclipboard(link) ok = true end) end
	if btn and orig then
		btn.Text = ok and "COPIED!" or "COPY FAILED"
		task.delay(1.6, function()
			if btn.Parent then btn.Text = orig end
		end)
	end
	return ok
end

-- ===================== WELCOME (Early Access Notice) =====================
local function buildWelcome()
	local welcome = Instance.new("Frame")
	welcome.Name = "Welcome"
	welcome.Size = UDim2.new(0, 380, 0, 520)
	welcome.Position = UDim2.new(0.5, -190, 0.5, -260)
	welcome.BackgroundColor3 = Color3.fromRGB(58, 33, 20)
	welcome.BorderSizePixel = 0
	welcome.Active = true
	welcome.ClipsDescendants = true
	welcome.Parent = gui
	corner(welcome, 22)
	stroke(welcome, Color3.new(1,1,1), 3)

	-- Header (same sky style)
	local wHeader = Instance.new("Frame")
	wHeader.Name = "Header"
	wHeader.Active = true
	wHeader.Size = UDim2.new(1, 0, 0, 112)
	wHeader.BackgroundColor3 = Color3.fromRGB(110, 207, 255)
	wHeader.BorderSizePixel = 0
	wHeader.Parent = welcome
	corner(wHeader, 22)

	local wFix = Instance.new("Frame")
	wFix.Size = UDim2.new(1, 0, 0, 24)
	wFix.Position = UDim2.new(0, 0, 1, -24)
	wFix.BackgroundColor3 = Color3.fromRGB(110, 207, 255)
	wFix.BorderSizePixel = 0
	wFix.Parent = wHeader

	local wGrad = Instance.new("UIGradient")
	wGrad.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(105, 200, 255)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(190, 245, 255))
	})
	wGrad.Rotation = 90
	wGrad.Parent = wHeader

	local wSun = Instance.new("TextLabel")
	wSun.BackgroundTransparency = 1
	wSun.Text = "☀️"
	wSun.TextSize = 34
	wSun.Size = UDim2.new(0, 44, 0, 44)
	wSun.Position = UDim2.new(1, -52, 0, 6)
	wSun.Parent = wHeader

	local wCloud1 = Instance.new("TextLabel")
	wCloud1.BackgroundTransparency = 1
	wCloud1.Text = "☁️"
	wCloud1.TextSize = 26
	wCloud1.TextTransparency = 0.15
	wCloud1.Size = UDim2.new(0, 40, 0, 30)
	wCloud1.Position = UDim2.new(0, 90, 0, 4)
	wCloud1.Parent = wHeader

	local wCloud2 = Instance.new("TextLabel")
	wCloud2.BackgroundTransparency = 1
	wCloud2.Text = "☁️"
	wCloud2.TextSize = 20
	wCloud2.TextTransparency = 0.3
	wCloud2.Size = UDim2.new(0, 34, 0, 26)
	wCloud2.Position = UDim2.new(0, 170, 0, 16)
	wCloud2.Parent = wHeader

	-- Avatar
	local wAvatar = Instance.new("Frame")
	wAvatar.Size = UDim2.new(0, 54, 0, 54)
	wAvatar.Position = UDim2.new(0, 14, 0, 14)
	wAvatar.BackgroundColor3 = Color3.fromRGB(255, 246, 229)
	wAvatar.BorderSizePixel = 0
	wAvatar.Parent = wHeader
	corner(wAvatar, 27)
	stroke(wAvatar, Color3.fromRGB(58,33,20), 2)

	local wAvatarImg = Instance.new("ImageLabel")
	wAvatarImg.BackgroundTransparency = 1
	wAvatarImg.Size = UDim2.new(1, -6, 1, -6)
	wAvatarImg.Position = UDim2.new(0, 3, 0, 3)
	wAvatarImg.Parent = wAvatar
	corner(wAvatarImg, 24)

	local wAvatarEm = Instance.new("TextLabel")
	wAvatarEm.BackgroundTransparency = 1
	wAvatarEm.Size = UDim2.new(1,0,1,0)
	wAvatarEm.Text = "🐶"
	wAvatarEm.TextSize = 28
	wAvatarEm.Parent = wAvatar

	pcall(function()
		local t = Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
		if typeof(t) == "string" and #t > 0 then
			wAvatarImg.Image = t
			wAvatarEm.Visible = false
		end
	end)

	-- Title
	local wTitle = Instance.new("TextLabel")
	wTitle.BackgroundTransparency = 1
	wTitle.Position = UDim2.new(0, 76, 0, 10)
	wTitle.Size = UDim2.new(0, 220, 0, 30)
	wTitle.Text = "ddariuspr"
	wTitle.TextSize = 26
	wTitle.Font = Enum.Font.FredokaOne
	wTitle.TextColor3 = Color3.new(1,1,1)
	wTitle.TextXAlignment = Enum.TextXAlignment.Left
	wTitle.Parent = wHeader

	local wTitleStroke = Instance.new("UIStroke")
	wTitleStroke.Color = Color3.fromRGB(40,20,10)
	wTitleStroke.Thickness = 2.5
	wTitleStroke.Parent = wTitle

	local wTitleGrad = Instance.new("UIGradient")
	wTitleGrad.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255,110,150)),
		ColorSequenceKeypoint.new(0.25, Color3.fromRGB(255,200,80)),
		ColorSequenceKeypoint.new(0.5, Color3.fromRGB(140,255,140)),
		ColorSequenceKeypoint.new(0.75, Color3.fromRGB(120,200,255)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(200,140,255))
	})
	wTitleGrad.Parent = wTitle

	local wBadge = Instance.new("TextLabel")
	wBadge.Position = UDim2.new(0, 210, 0, 16)
	wBadge.Size = UDim2.new(0, 90, 0, 20)
	wBadge.Text = "EARLY ACCESS"
	wBadge.TextSize = 11
	wBadge.Font = Enum.Font.GothamBlack
	wBadge.TextColor3 = Color3.fromRGB(58,33,20)
	wBadge.BackgroundColor3 = Color3.fromRGB(255, 180, 60)
	wBadge.Parent = wHeader
	corner(wBadge, 10)
	stroke(wBadge, Color3.new(1,1,1), 2)

	local wUser = Instance.new("TextLabel")
	wUser.BackgroundTransparency = 1
	wUser.Position = UDim2.new(0, 77, 0, 40)
	wUser.Size = UDim2.new(0, 220, 0, 16)
	wUser.Text = "YouTube Dev • Keyless Scripts"
	wUser.TextSize = 11
	wUser.Font = Enum.Font.GothamBold
	wUser.TextColor3 = Color3.fromRGB(58,33,20)
	wUser.TextTransparency = 0.25
	wUser.TextXAlignment = Enum.TextXAlignment.Left
	wUser.Parent = wHeader

	local wPill = Instance.new("Frame")
	wPill.Size = UDim2.new(0, 210, 0, 22)
	wPill.Position = UDim2.new(0, 76, 0, 58)
	wPill.BackgroundColor3 = Color3.fromRGB(40, 22, 12)
	wPill.BorderSizePixel = 0
	wPill.Parent = wHeader
	corner(wPill, 11)

	local wPillTxt = Instance.new("TextLabel")
	wPillTxt.BackgroundTransparency = 1
	wPillTxt.Size = UDim2.new(1,-12,1,0)
	wPillTxt.Position = UDim2.new(0,12,0,0)
	wPillTxt.Text = "● THIS WAS JUST EARLY ACCESS"
	wPillTxt.TextSize = 11
	wPillTxt.Font = Enum.Font.GothamBold
	wPillTxt.TextColor3 = Color3.fromRGB(255, 200, 80)
	wPillTxt.TextXAlignment = Enum.TextXAlignment.Left
	wPillTxt.Parent = wPill

	local wGrass = Instance.new("Frame")
	wGrass.Size = UDim2.new(1, 0, 0, 16)
	wGrass.Position = UDim2.new(0, 0, 1, -16)
	wGrass.BackgroundColor3 = Color3.fromRGB(90, 203, 77)
	wGrass.BorderSizePixel = 0
	wGrass.Parent = wHeader

	local wFlowers = Instance.new("TextLabel")
	wFlowers.BackgroundTransparency = 1
	wFlowers.Size = UDim2.new(1,-10,1,0)
	wFlowers.Position = UDim2.new(0,5,0,0)
	wFlowers.Text = "🌸     🍀     🌼     🍀     🌸"
	wFlowers.TextSize = 10
	wFlowers.TextXAlignment = Enum.TextXAlignment.Left
	wFlowers.Parent = wGrass

	-- Body
	local wBody = Instance.new("ScrollingFrame")
	wBody.Size = UDim2.new(1, -24, 0, 340)
	wBody.Position = UDim2.new(0, 12, 0, 122)
	wBody.BackgroundTransparency = 1
	wBody.BorderSizePixel = 0
	wBody.ScrollBarThickness = 4
	wBody.ScrollBarImageColor3 = Color3.fromRGB(175, 255, 51)
	wBody.CanvasSize = UDim2.new(0, 0, 0, 0)
	wBody.AutomaticCanvasSize = Enum.AutomaticSize.Y
	wBody.Parent = welcome

	local wList = Instance.new("UIListLayout")
	wList.Padding = UDim.new(0, 12)
	wList.SortOrder = Enum.SortOrder.LayoutOrder
	wList.Parent = wBody

	-- Hero card
	local wHero = Instance.new("Frame")
	wHero.Size = UDim2.new(1, 0, 0, 130)
	wHero.BackgroundColor3 = Color3.fromRGB(255, 246, 229)
	wHero.BorderSizePixel = 0
	wHero.LayoutOrder = 1
	wHero.ClipsDescendants = true
	wHero.Parent = wBody
	corner(wHero, 18)
	stroke(wHero, Color3.new(1,1,1), 2)

	local wEgg = Instance.new("Frame")
	wEgg.Size = UDim2.new(0, 58, 0, 58)
	wEgg.Position = UDim2.new(0, 12, 0, 12)
	wEgg.BackgroundColor3 = Color3.fromRGB(255, 158, 44)
	wEgg.BorderSizePixel = 0
	wEgg.Parent = wHero
	corner(wEgg, 18)
	stroke(wEgg, Color3.fromRGB(58,33,20), 2)

	local wEggTxt = Instance.new("TextLabel")
	wEggTxt.BackgroundTransparency = 1
	wEggTxt.Size = UDim2.new(1,0,1,0)
	wEggTxt.Text = "🥚"
	wEggTxt.TextSize = 30
	wEggTxt.Parent = wEgg

	local wHeroTitle = Instance.new("TextLabel")
	wHeroTitle.BackgroundTransparency = 1
	wHeroTitle.Position = UDim2.new(0, 80, 0, 10)
	wHeroTitle.Size = UDim2.new(1, -92, 0, 22)
	wHeroTitle.Text = "Early Access Version"
	wHeroTitle.TextSize = 16
	wHeroTitle.Font = Enum.Font.FredokaOne
	wHeroTitle.TextColor3 = Color3.fromRGB(58,33,20)
	wHeroTitle.TextXAlignment = Enum.TextXAlignment.Left
	wHeroTitle.Parent = wHero

	local wHeroDesc = Instance.new("TextLabel")
	wHeroDesc.BackgroundTransparency = 1
	wHeroDesc.Position = UDim2.new(0, 80, 0, 34)
	wHeroDesc.Size = UDim2.new(1, -92, 0, 80)
	wHeroDesc.Text = "This was just an early-access build I shared with a few people.\nThe full release is out now with way more toggles & features.\nGrab the real one below 👇"
	wHeroDesc.TextSize = 12
	wHeroDesc.Font = Enum.Font.GothamBold
	wHeroDesc.TextColor3 = Color3.fromRGB(150, 115, 85)
	wHeroDesc.TextWrapped = true
	wHeroDesc.TextXAlignment = Enum.TextXAlignment.Left
	wHeroDesc.TextYAlignment = Enum.TextYAlignment.Top
	wHeroDesc.Parent = wHero

	-- About Me card
	local aboutCard = Instance.new("Frame")
	aboutCard.Size = UDim2.new(1, 0, 0, 160)
	aboutCard.BackgroundColor3 = Color3.fromRGB(255, 246, 229)
	aboutCard.BorderSizePixel = 0
	aboutCard.LayoutOrder = 2
	aboutCard.Parent = wBody
	corner(aboutCard, 18)
	stroke(aboutCard, Color3.new(1,1,1), 2)

	local aboutTitle = Instance.new("TextLabel")
	aboutTitle.BackgroundTransparency = 1
	aboutTitle.Position = UDim2.new(0, 16, 0, 10)
	aboutTitle.Size = UDim2.new(1, -32, 0, 22)
	aboutTitle.Text = "👋 Who is ddariuspr?"
	aboutTitle.TextSize = 15
	aboutTitle.Font = Enum.Font.FredokaOne
	aboutTitle.TextColor3 = Color3.fromRGB(58,33,20)
	aboutTitle.TextXAlignment = Enum.TextXAlignment.Left
	aboutTitle.Parent = aboutCard

	local aboutTxt = Instance.new("TextLabel")
	aboutTxt.BackgroundTransparency = 1
	aboutTxt.Position = UDim2.new(0, 16, 0, 36)
	aboutTxt.Size = UDim2.new(1, -32, 0, 110)
	aboutTxt.Text = "I'm a YouTube developer who drops clean, keyless scripts for Roblox games.\n\nNo keys. No paywalls. Just pure scripts that actually work.\n\nI make everything myself — from the farm logic to the pretty UI you see here.\nIf you like this style, the full Ride A Pet release is already live."
	aboutTxt.TextSize = 12
	aboutTxt.Font = Enum.Font.Gotham
	aboutTxt.TextColor3 = Color3.fromRGB(120, 90, 60)
	aboutTxt.TextWrapped = true
	aboutTxt.TextXAlignment = Enum.TextXAlignment.Left
	aboutTxt.TextYAlignment = Enum.TextYAlignment.Top
	aboutTxt.Parent = aboutCard

	-- Buttons
	local function makeLinkBtn(order, icon, title, btnText, color, link)
		local card = Instance.new("Frame")
		card.Size = UDim2.new(1, 0, 0, 70)
		card.BackgroundColor3 = Color3.fromRGB(255, 246, 229)
		card.BorderSizePixel = 0
		card.LayoutOrder = order
		card.Parent = wBody
		corner(card, 16)
		stroke(card, Color3.new(1,1,1), 2)

		local ic = Instance.new("Frame")
		ic.Size = UDim2.new(0, 44, 0, 44)
		ic.Position = UDim2.new(0, 12, 0, 13)
		ic.BackgroundColor3 = color
		ic.BorderSizePixel = 0
		ic.Parent = card
		corner(ic, 13)
		stroke(ic, Color3.fromRGB(58,33,20), 2)

		local ict = Instance.new("TextLabel")
		ict.BackgroundTransparency = 1
		ict.Size = UDim2.new(1,0,1,0)
		ict.Text = icon
		ict.TextSize = 22
		ict.Parent = ic

		local nm = Instance.new("TextLabel")
		nm.BackgroundTransparency = 1
		nm.Position = UDim2.new(0, 66, 0, 10)
		nm.Size = UDim2.new(1, -78, 0, 20)
		nm.Text = title
		nm.TextSize = 14
		nm.Font = Enum.Font.FredokaOne
		nm.TextColor3 = Color3.fromRGB(58,33,20)
		nm.TextXAlignment = Enum.TextXAlignment.Left
		nm.Parent = card

		local btn = Instance.new("TextButton")
		btn.Size = UDim2.new(1, -78, 0, 26)
		btn.Position = UDim2.new(0, 66, 0, 34)
		btn.Text = btnText
		btn.TextSize = 12
		btn.Font = Enum.Font.GothamBlack
		btn.TextColor3 = Color3.new(1,1,1)
		btn.BackgroundColor3 = color
		btn.Parent = card
		corner(btn, 8)
		stroke(btn, Color3.fromRGB(58,33,20), 1)

		btn.Activated:Connect(function()
			copyLink(link, btn, btnText)
		end)

		return card, btn
	end

	makeLinkBtn(3, "🔴", "Get the full release on YouTube", "TAP TO COPY YOUTUBE LINK", Color3.fromRGB(255, 80, 80), YT_LINK)
	makeLinkBtn(4, "💜", "Or grab it in Discord", "TAP TO COPY DISCORD INVITE", Color3.fromRGB(120, 120, 255), DISCORD_LINK)

	-- Footer note
	local note = Instance.new("TextLabel")
	note.BackgroundTransparency = 1
	note.Size = UDim2.new(1, 0, 0, 50)
	note.LayoutOrder = 5
	note.Text = "The full version looks exactly like this GUI\nbut with all the new toggles unlocked.\nThanks for supporting 💖"
	note.TextSize = 12
	note.Font = Enum.Font.GothamBold
	note.TextColor3 = Color3.fromRGB(255, 231, 170)
	note.TextTransparency = 0.15
	note.TextWrapped = true
	note.Parent = wBody

	-- Footer bar
	local wFooter = Instance.new("Frame")
	wFooter.Size = UDim2.new(1, -24, 0, 48)
	wFooter.Position = UDim2.new(0, 12, 1, -56)
	wFooter.BackgroundColor3 = Color3.fromRGB(40, 22, 12)
	wFooter.BorderSizePixel = 0
	wFooter.Parent = welcome
	corner(wFooter, 14)

	local wCash = Instance.new("TextLabel")
	wCash.BackgroundTransparency = 1
	wCash.Position = UDim2.new(0, 12, 0, 4)
	wCash.Size = UDim2.new(1, -24, 0, 20)
	wCash.Text = "ddariuspr • Early Access"
	wCash.TextSize = 16
	wCash.Font = Enum.Font.FredokaOne
	wCash.TextColor3 = Color3.fromRGB(175, 255, 51)
	wCash.TextXAlignment = Enum.TextXAlignment.Left
	wCash.Parent = wFooter

	local wFootSub = Instance.new("TextLabel")
	wFootSub.BackgroundTransparency = 1
	wFootSub.Position = UDim2.new(0, 12, 0, 26)
	wFootSub.Size = UDim2.new(1, -24, 0, 16)
	wFootSub.Text = "tapping the buttons copies the link"
	wFootSub.TextSize = 11
	wFootSub.Font = Enum.Font.GothamBold
	wFootSub.TextColor3 = Color3.fromRGB(255, 231, 170)
	wFootSub.TextTransparency = 0.25
	wFootSub.TextXAlignment = Enum.TextXAlignment.Left
	wFootSub.Parent = wFooter

	-- Animations
	task.spawn(function()
		local hue = 0
		while welcome.Parent do
			hue = (hue + 0.01) % 1
			wTitleGrad.Offset = Vector2.new(hue, 0)
			wSun.Rotation = (wSun.Rotation + 1) % 360
			task.wait(0.06)
		end
	end)

	task.spawn(function()
		while welcome.Parent do
			TweenService:Create(wCloud1, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Position = UDim2.new(0, 130, 0, 4)}):Play()
			TweenService:Create(wCloud2, TweenInfo.new(4, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Position = UDim2.new(0, 210, 0, 16)}):Play()
			TweenService:Create(wEgg, TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Rotation = 10}):Play()
			task.wait(3.1)
			if not welcome.Parent then break end
			TweenService:Create(wCloud1, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Position = UDim2.new(0, 90, 0, 4)}):Play()
			TweenService:Create(wCloud2, TweenInfo.new(4, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Position = UDim2.new(0, 170, 0, 16)}):Play()
			TweenService:Create(wEgg, TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Rotation = -10}):Play()
			task.wait(3.1)
		end
	end)

	-- Make draggable
	local dragging = false
	local dragStart = Vector2.zero
	local startPos = UDim2.new()
	wHeader.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = welcome.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then dragging = false end
			end)
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local d = input.Position - dragStart
			welcome.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y)
		end
	end)

	return welcome
end

buildWelcome()

print("[ddariuspr] Early Access notice loaded — directing users to the full release")
